AC_SUBST([AC3DEC_CFLAGS])

AC_ARG_ENABLE([gprof],[  --enable-gprof          make a version using gprof])
if test x"$enable_gprof" = x"yes"; then
    AC_DEFINE([AC3DEC_GPROF],,[ac3dec profiling])
    AC3DEC_CFLAGS="$AC3DEC_CFLAGS -p"
fi
